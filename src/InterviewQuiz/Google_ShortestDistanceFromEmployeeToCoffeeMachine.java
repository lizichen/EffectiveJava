package InterviewQuiz;

// Google onsite, Dec 6, 2018, Round 2.
//
// Given a 2-by-2 matrix representing an office, where
// matrix[i][j] = 'E', refers to an employee's location
// matrix[i][j] = 'P', refers to a printer's location
// matrix[i][j] = empty, refers that nothing's there.
//
// Now, find a optimal position in matrix(the office) to put a coffee machine.
public class Google_ShortestDistanceFromEmployeeToCoffeeMachine {
}
